language: android

android:
  components:
    - tools
    - platform-tools
    - build-tools-28.0.3
    - android-25
    - extra-google-m2repository
    - extra-android-m2repository

cache:
  directories:
    - "$HOME/.gradle/wrapper/"

script:
  - ./gradlew --no-daemon build
